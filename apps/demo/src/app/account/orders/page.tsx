export default function OrdersPage() {
  return (
    <div>
      <h1>Order History</h1>
      <p style={{ color: '#666' }}>
        Order history will be rendered using the useOrders hook with server-side data.
      </p>
    </div>
  );
}
